<div id="<%= dom_id post %>">
  <p>
    <strong>Author</strong>
    <%= post.user.email if user_signed_in? %>
  </p>
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

</div>
